prepare:
  sample_size: 1000
  validation_split: 0.2
  random_seed: 42

labeling:
  export_format: "json"
  include_metadata: true
  annotation_schema: "classification"

validation:
  min_samples: 100
  quality_threshold: 0.95
  required_labels: ["positive", "negative", "neutral"]